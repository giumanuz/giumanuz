cache/
.venv/
.env
.DS_Store
__pycache__/

# today.py runs inside the container as `git add .` followed by `git push
# --force`, so anything left lying in the working copy gets published. Keep
# host-local files out of its reach.
logos/avatar.jpg
.no-autodeploy